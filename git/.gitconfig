# Shared git config — symlinked to ~/.gitconfig by install.sh.
# Personal details (name, email, signing key) go in ~/.gitconfig.local.
# Copy git/.gitconfig.local.example as a starting point.

[pull]
	rebase = true
[gpg]
	format = ssh
[include]
	path = ~/.gitconfig.local
