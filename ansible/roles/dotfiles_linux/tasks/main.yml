---
# ---------------------------------------------------------------------------
# RETIRED — replaced by roles/dotfiles/
# ---------------------------------------------------------------------------
# This role previously pushed dotfiles from the Mac control node and created
# symlinks on Linux servers. Both responsibilities have been moved:
#
#   Content management → git clone in roles/dotfiles/
#   Symlink creation   → install.sh (run by roles/dotfiles/)
#
# This file is intentionally empty. The role directory is kept to avoid
# breaking any external references, but it is no longer called by linux.yml.
# ---------------------------------------------------------------------------
