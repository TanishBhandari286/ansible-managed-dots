---
# ---------------------------------------------------------------------------
# RETIRED — replaced by roles/dotfiles/
# ---------------------------------------------------------------------------
# This role previously created macOS-specific symlinks. Symlink creation has
# been moved to install.sh (run by roles/dotfiles/ after cloning the repo).
#
# This file is intentionally empty. The role directory is kept to avoid
# breaking any external references, but it is no longer called by mac.yml.
# ---------------------------------------------------------------------------
